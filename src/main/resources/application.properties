spring.application.name=Forproject

#  DATABASE CONFIG
spring.datasource.url=jdbc:mysql://localhost:3306/coursesphere?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=2400030993

#  JPA / HIBERNATE
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

#  SERVER PORT (keep 8080)
server.port=3366

#  OPTIONAL (better debugging)
logging.level.org.springframework=INFO